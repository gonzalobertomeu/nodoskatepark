---
/**
 * Post-login landing. The shell replaces this address with the first destination of the account's
 * role (FR-013) — no intermediate screen, no extra tap.
 */
import { AppShell } from '../components/app-shell/AppShell';
import AppLayout from '../layouts/AppLayout.astro';
---

<AppLayout title="NodoSkatepark">
  <AppShell initialPath="/" client:load />
</AppLayout>
